
export enum EShapeEmitterMode {
    Box = 1,
    Cone = 2,
    Hemisphere = 3,
    Sphere = 4,
    Point = 5,
    Edge = 6,
    Circle = 7,
    Rectangle = 8,
}

export enum EArcMode {
    Random = 0,
    Loop = 1,
    PingPong = 2,
    BurstsDpread = 3,
}

/**
 * 形状发射器创建模式
 */
 export enum EShapeEmitterDirectionMode {
    Unity = 0,
}